# Calculate format=contents pkg(sys-apps/openrc)!= ini(overlay-calculate.revision)<#-cut(0,-,#-cl_pass_file-#)-#

#-ini(overlay-calculate.revision,#-cut(0,-,#-cl_pass_file-#)-#)-#
!sys-apps/baselayout /etc/hosts
!sys-apps/openrc /etc/conf.d/hostname
!sys-apps/openrc /etc/conf.d/net
