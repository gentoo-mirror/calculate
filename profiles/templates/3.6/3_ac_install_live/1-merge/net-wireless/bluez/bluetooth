# Calculate link=/etc/init.d/bluetooth symbolic path=/etc/runlevels/default ini(runlevels.bluez)!=on&&ini(runlevels.bluez,on)==&&os_audio!=alsa||grep(/var/lib/calculate/-runlevels-bluez,/bluetooth)!=
