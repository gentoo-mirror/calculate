# Calculate exec=/bin/bash cl_ebuild_phase==postinst&&os_root_type!=livecd&&cl_chroot_status==off
# skipping cl-setup-system, cl-setup-boot, cl-core-setup -n grub, installation in chroot or livecd

cl-setup-boot
