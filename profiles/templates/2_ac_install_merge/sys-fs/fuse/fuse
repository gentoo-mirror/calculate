# Calculate link=/etc/init.d/fuse symbolic path=/etc/runlevels/default os_install_linux_system==desktop&&ini(runlevels.fuse)!=on&&ini(runlevels.fuse,on)==||grep(/var/lib/calculate/-runlevels-fuse,/fuse)!=
