# Calculate run=/bin/bash ini(overlay-calculate.revision)<#-cut(0,-,#-cl_pass_file-#)-#&&cl_chroot_status==on

#-ini(overlay-calculate.revision,#-cut(0,-,#-cl_pass_file-#)-#)-#
#
env-update
for i in /usr/share/info/*.info*;do install-info --info-dir=/usr/share/info $i;done

true
