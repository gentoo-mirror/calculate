# Calculate ini(overlay-calculate.revision)<48 exec=/bin/bash

#-ini(overlay-calculate.revision,48)-#

#?pkg(sys-kernel/dracut)!=&&pkg(media-gfx/splashutils)!=&&main.cl_chroot_status==on#
/usr/bin/dracut -f &>/dev/null
#pkg#

exit 0
