# Calculate ini(overlay-calculate.revision)<36 exec=/bin/bash

#-ini(overlay-calculate.revision,36)-#

#?pkg(sys-kernel/dracut)!=&&pkg(sys-boot/plymouth)!=&&main.cl_chroot_status==on#
/usr/bin/dracut -f &>/dev/null
#pkg#

exit 0
