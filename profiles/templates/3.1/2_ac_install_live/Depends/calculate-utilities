# Calculate append=skip merge(calculate-utilities)!=||merge(calculate-install)!=||merge(calculate-desktop)!=||merge(calculate-client)!=||merge(calculate-core)!= merge=libreoffice,gdm,gnome-shell,xkeyboard-config
