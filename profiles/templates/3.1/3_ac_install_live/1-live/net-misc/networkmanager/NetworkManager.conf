# Calculate cl_ver>=3.1.2 format=samba path=/etc/NetworkManager

[main]
# условие выключения интерфейсов
no-auto-default=#?list(os_install_net_interfaces,0)==||os_install_net_conf!=networkmanager||list(os_install_net_status)==off&&cl_merge_pkg!=||list(os_install_net_status)==off&&ac_install_disk==on||list(os_install_net_status)==off&&cl_setup==network##-list(os_install_net_mac,0)-#,#list##?list(os_install_net_interfaces,1)==||os_install_net_conf!=networkmanager||list(os_install_net_status)==off&&cl_merge_pkg!=||list(os_install_net_status)==off&&ac_install_disk==on||list(os_install_net_status)==off&&cl_setup==network##-list(os_install_net_mac,1)-#,#list##?list(os_install_net_interfaces,2)==||os_install_net_conf!=networkmanager||list(os_install_net_status)==off&&cl_merge_pkg!=||list(os_install_net_status)==off&&ac_install_disk==on||list(os_install_net_status)==off&&cl_setup==network##-list(os_install_net_mac,2)-#,#list##?list(os_install_net_interfaces,3)==||os_install_net_conf!=networkmanager||list(os_install_net_status)==off&&cl_merge_pkg!=||list(os_install_net_status)==off&&ac_install_disk==on||list(os_install_net_status)==off&&cl_setup==network##-list(os_install_net_mac,3)-#,#list#
