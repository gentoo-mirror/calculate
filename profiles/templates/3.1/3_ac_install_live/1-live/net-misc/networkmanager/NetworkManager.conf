# Calculate cl_ver>=3.1.2 format=samba path=/etc/NetworkManager

[main]
# условие выключения интерфейсов
no-auto-default=#?list(os_install_net_interfaces,0)==||os_install_net_conf!=networkmanager||list(os_install_net_status,0)==off&&os_install_root_type!=livecd||list(os_install_net_status,0)==off&&cl_setup==network##-list(os_install_net_mac,0)-#,#list##?list(os_install_net_interfaces,1)==||os_install_net_conf!=networkmanager||list(os_install_net_status,1)==off&&os_install_root_type!=livecd||list(os_install_net_status,1)==off&&cl_setup==network##-list(os_install_net_mac,1)-#,#list##?list(os_install_net_interfaces,2)==||os_install_net_conf!=networkmanager||list(os_install_net_status,2)==off&&os_install_root_type!=livecd||list(os_install_net_status,2)==off&&cl_setup==network##-list(os_install_net_mac,2)-#,#list##?list(os_install_net_interfaces,3)==||os_install_net_conf!=networkmanager||list(os_install_net_status,3)==off&&os_install_root_type!=livecd||list(os_install_net_status,3)==off&&cl_setup==network##-list(os_install_net_mac,3)-##list#
