# Calculate ini(update.rev)<51 format=samba path=/etc/calculate name=ini.env merge=sys-apps/openrc

# updating init.sh in sys-apps/openrc to fix the rare boot fail, reconfigure rc.conf for nscd
[update]
rev = 51
