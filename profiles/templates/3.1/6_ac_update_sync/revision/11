# Calculate path=/usr/lib/calculate-2.2/calculate-builder/pym name=cl_kernel.py ini(update.rev)<11&&pkg(sys-apps/calculate-builder)<2.2.31-r6 format=patch

#-ini(update.rev,11)-#

<reg>if filter\(lambda x:x.startswith\("grub-1.99"\),</reg>
<text>if filter(lambda x:x.startswith("grub-1.99") or x.startswith("grub-2"),</text>
