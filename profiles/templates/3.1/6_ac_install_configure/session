# Calculate format=samba merge=x11-base/xorg-server cl_setup==session||cl_setup== path=/etc/calculate name=calculate.env

[main]
cl_home_crypt_set = #-cl_install_home_crypt_set-#

[install]
cl_autologin = #-cl_autologin-#
