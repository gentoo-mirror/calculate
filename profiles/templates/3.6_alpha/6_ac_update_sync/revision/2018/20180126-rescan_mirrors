# Calculate format=samba ini(overlay-calculate.revision)<#-cut(0,-,#-cl_pass_file-#)-# name=calculate.env 

#-ini(overlay-calculate.revision,#-cut(0,-,#-cl_pass_file-#)-#)-#

#?cl_chroot_status==off#
[update]
!cl_update_binhost=
