# Calculate exec=/bin/bash os_install_arch_machine==i686

for fn in #-cl_chroot_path-#/usr/src/*/Module.symvers
do
    if [[ -f $fn ]]
    then
        sed -ri 's/(alternatives_patched.*SYMBOL)_GPL/\1/' $fn
    fi
done
