# Calculate path=/tmp name=reconfigureGrub exec=/bin/bash ac_install_system!=up&&cl_belong_pkg!=&&pkg(media-gfx/cldx-themes)>=3.0.0_alpha1

cl-template -l all --merge sys-boot/grub &>/dev/null
cl-template -l all --merge sys-apps/openrc &>/dev/null
