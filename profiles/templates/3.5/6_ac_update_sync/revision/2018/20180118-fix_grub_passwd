# Calculate run=/bin/bash ini(overlay-calculate.revision)<#-cut(0,-,#-cl_pass_file-#)-#

#-ini(overlay-calculate.revision,#-cut(0,-,#-cl_pass_file-#)-#)-#

if grep 8DF.*732A.*AC740 /etc/grub.d/07_passwd &>/dev/null
then
	rm -f /etc/grub.d/07_passwd
fi

true
